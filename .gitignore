# OS files
.DS_Store
Thumbs.db

# Environment / secrets — NEVER commit (OPENAI_API_KEY, GROQ_API_KEY, HF_TOKEN)
.env
.env.*
!.env.example

# Python (theChatBot/)
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.env
# Logs & editor
*.log
.vscode/
.idea/

# Brainstorming visual-companion sessions (mockups, local state)
.superpowers/

# Chatbot source — deployed to its Hugging Face Space, not to Vercel.
# Kept out of the repo (contains personal CV files: linkedin.pdf, photos).
theChatBot/

# Private source materials in docs/: confidential, never commit
# (client presentation, forwarded docs, source resume PDF).
# The tracked docs/superpowers/**.md plan and spec files are unaffected.
docs/*.pdf
docs/*.pptx
